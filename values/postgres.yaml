auth:
  username: postgres
  password: postgres

customUser:
  name: keycloak
  password: keycloak

service:
  type: NodePort

config:
  postgresqlLogStatement: all
  extraConfig:
    - logging_collector = on
    - log_directory = '/var/log/postgresql'
